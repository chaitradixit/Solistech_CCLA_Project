ALTER TABLE CCLA_CAMPAIGNS ADD FUND_CODE VARCHAR2(2 CHAR)

ALTER TABLE CCLA_CAMPAIGNS ADD CONSTRAINT CCLA_CAMPAIGNS_FK FOREIGN KEY (FUND_CODE) REFERENCES FUND (FUND_CODE);


alter table ccla_process rename COLUMN PREF_CONTACT_METHOD to CONTACT_ADDRESS_ID;

UPDATE CCLA_PROCESS SET CONTACT_ADDRESS_ID = NULL;
ALTER TABLE CCLA_PROCESS modify (CONTACT_ADDRESS_ID number(15,0));

ALTER TABLE ccla_process ADD CONSTRAINT ccla_process_address_FK FOREIGN KEY (CONTACT_ADDRESS_ID) REFERENCES ADDRESS (ADDRESS_ID);
